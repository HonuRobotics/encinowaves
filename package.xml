<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>ehukai</name>
  <version>0.0.2</version>
  <description>
    Headless spectral ocean-wave synthesis library: TMA/JONSWAP/Pierson-Moskowitz
    spectra with directional spreading and dispersion, after Horvath (2015).
    Exports the ehukai::ehukai CMake target.

    This manifest exists so the library can be developed as a peer of its
    consumers in a colcon workspace. It is a plain CMake package — colcon does
    not change how it is configured, built or installed, and it is not a ROS
    package in any other sense. The Debian packaging (cmake/Packaging.cmake) and
    a standalone cmake build are unaffected.
  </description>
  <maintainer email="info@honurobotics.com">Honu Robotics</maintainer>
  <license>Apache-2.0</license>
  <url type="repository">https://github.com/HonuRobotics/ehukai</url>

  <buildtool_depend>cmake</buildtool_depend>

  <!-- These are rosdep KEYS, not apt package names: `eigen` resolves to
       libeigen3-dev and `tbb` to libtbb-dev. Version floors live in
       CMakeLists.txt (EHUKAI_EIGEN3_MIN, EHUKAI_TBB_MIN, EHUKAI_IMATH_MIN);
       rosdep keys carry no version, so the two are matched by hand.

       Imath has NO rosdep key — there is no rule for imath, ilmbase or
       openexr in any rosdistro list — so it cannot be declared here and
       `rosdep install` will not install it. Install libimath-dev yourself
       (the README's build instructions do), or use a container that carries
       it. Declaring the apt name directly fails with "Cannot locate rosdep
       definition". -->
  <depend>eigen</depend>
  <depend>tbb</depend>

  <export>
    <build_type>cmake</build_type>
  </export>
</package>
